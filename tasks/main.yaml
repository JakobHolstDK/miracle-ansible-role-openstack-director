---
- name: Set a hostname
  ansible.builtin.hostname:
    name: director

- name: Creates directory
  file:
    path: /home/stack/templates
    state: directory
    owner: stack 
    group: stack 
  become: True

- name: Creates directory
  file:
    path: /home/stack/images
    state: directory
    owner: stack 
    group: stack 
  become: True
